[build]
  command = """
    curl -fsSL https://bun.sh/install | bash
    && export PATH=$HOME/.bun/bin:$PATH
    && bun install
    && bun build
  """
  publish = "build" # or "dist" or whatever your output folder is